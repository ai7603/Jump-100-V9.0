using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using System.IO;
using System;

using UnityEngine.SceneManagement;


public class LoginToMain : MonoBehaviour {

    public TextReader inputText;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        /*if (Input.GetKey(KeyCode.KeypadEnter) || Input.GetKey(KeyCode.Return) || Input.GetKey(KeyCode.Space)) {
            UserInfo.USER = inputText.text;
            UserInfo.COIN = GetCoin(inputText.text);
            UserInfo.PETS = HasPets(inputText.text);
            SceneManager.LoadScene("Main");
        }*/
       
    }
}
